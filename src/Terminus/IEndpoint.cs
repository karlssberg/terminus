namespace Terminus;

/// <summary>
/// Marker interface for endpoint classes. Classes implementing this interface can contain
/// methods decorated with endpoint attributes to define service endpoints.
/// </summary>
public interface IEndpoint
{
}
